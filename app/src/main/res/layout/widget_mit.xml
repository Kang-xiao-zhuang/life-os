<?xml version="1.0" encoding="utf-8"?>
<!--
  今日最重要 on the home screen — the point of a workbench is that you can glance at it.

  Two task lines, matching MIT_SOFT_LIMIT: this widget cannot show "everything", and if it tried it
  would just be a worse task list. Every string is set by the provider rather than baked in here,
  because the launcher inflates this layout with the *phone's* locale.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mit_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <TextView
        android:id="@+id/mit_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/task_mit"
        android:textColor="@color/widget_accent"
        android:textSize="12sp" />

    <!-- Shown only when there is nothing flagged, so the widget never looks broken. -->
    <TextView
        android:id="@+id/mit_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:textColor="@color/widget_text_dim"
        android:textSize="13sp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/mit_line1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:textColor="@color/widget_text"
        android:textSize="15sp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/mit_line2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/widget_text"
        android:textSize="15sp"
        android:visibility="gone" />
</LinearLayout>
