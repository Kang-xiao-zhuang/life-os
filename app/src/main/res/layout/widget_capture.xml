<?xml version="1.0" encoding="utf-8"?>
<!-- One tap → the capture field. No data shown, so the widget never needs updating. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:contentDescription="@string/widget_capture_label">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="＋"
        android:textColor="@color/widget_accent"
        android:textSize="20sp" />

    <!--
      android:text is the fallback the launcher inflates with; the provider overwrites it with the
      in-app language, which the launcher's own resources know nothing about.
    -->
    <TextView
        android:id="@+id/widget_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/widget_capture_label"
        android:textColor="@color/widget_text"
        android:textSize="15sp" />
</LinearLayout>
